{
  "name": "chatbot-ho-tro-hsth",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chatbot Hỗ Trợ Học Sinh Tự Học — Giáo viên xây dựng, phụ huynh quản lý, học sinh tự học",
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "build:server": "npm run build --workspace=server",
    "db:generate": "npm run db:generate --workspace=server",
    "db:migrate": "npm run db:migrate --workspace=server",
    "db:studio": "npm run db:studio --workspace=server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "better-auth": "^1.2.0"
  }
}
